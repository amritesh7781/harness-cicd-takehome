apiVersion: v1
kind: Service
metadata:
  name: harness-demo-app-svc
  labels:
    app: harness-demo-app
spec:
  type: ClusterIP
  selector:
    app: harness-demo-app
  ports:
    - port: 80
      targetPort: 8080
